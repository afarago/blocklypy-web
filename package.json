{
  "name": "",
  "version": "0.0.0",
  "description": "",
  "main": "build/src/index.js",
  "types": "build/src/index.d.ts",
  "type": "module",
  "files": [
    "build/src"
  ],
  "license": "Apache-2.0",
  "keywords": [],
  "scripts": {
    "dev": "npm run start:console:dev",
    "start:console": "tsx  ./src/console.ts",
    "start:console:dev": "nodemon --exec tsx ./src/console.ts",
    "start:server": "tsx  ./src/server.ts",
    "start:server:dev": "nodemon --exec tsx ./src/server.ts",
    "curl1": "curl -X POST -F \"file=@testdata/test2.lms\" http://localhost:3000/upload"
  },
  "OLD_scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm.cmd run compile",
    "pretest": "npm.cmd run compile",
    "posttest": "npm.cmd run lint",
    "dev:debug": "ts-node-dev --transpile-only --respawn --inspect=4321 --project tsconfig.json ./app.ts",
    "dev": "ts-node-dev --transpile-only --project tsconfig.json src/index.ts",
    "start": "concurrently \"nodemon --watch src -e ts --exec ts-node src/index.ts\" \"browser-sync start --config bs-config.js\"",
    "start:dev": "nodemon --watch src -e ts --exec npm start dev"
  },
  "devDependencies": {
    "@types/express-fileupload": "^1.5.1",
    "@types/jszip": "^3.4.1",
    "@types/node": "^20.16.5",
    "browser-sync": "^3.0.2",
    "concurrently": "^8.2.2",
    "gts": "^5.3.1",
    "ts-node": "^10.9.2",
    "tslib": "^2.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "@types/express": "^4.17.21",
    "express": "^4.19.2",
    "express-fileupload": "^1.5.1",
    "jszip": "^3.10.1"
  }
}
